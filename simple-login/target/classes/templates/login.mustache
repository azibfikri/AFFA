<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Login</title>
    <style>
        body {
            font-family: "Segoe UI", Arial, sans-serif;
            background: linear-gradient(135deg, #111d4a, #2575fc);
            margin: 0;
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            color: #1f2b50;
        }
        .card {
            width: 360px;
            padding: 32px;
            background: rgba(255,255,255,0.95);
            border-radius: 18px;
            box-shadow: 0 24px 60px rgba(12,26,75,0.35);
        }
        .card h1 {
            margin: 0;
            font-size: 26px;
            color: #132048;
        }
        .card p {
            margin-top: 6px;
            color: #5d6995;
        }
        .error {
            background: #ffe4e4;
            color: #c62828;
            padding: 10px 14px;
            border-radius: 8px;
            margin: 16px 0;
            font-size: 14px;
        }
        label {
            display: block;
            margin-top: 18px;
            font-weight: 600;
            font-size: 14px;
            color: #233366;
        }
        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 12px;
            border: 1px solid #d7daec;
            border-radius: 10px;
            margin-top: 6px;
            font-size: 15px;
            transition: border 0.2s ease, box-shadow 0.2s ease;
        }
        input:focus {
            outline: none;
            border-color: #4c7eff;
            box-shadow: 0 0 0 3px rgba(76,126,255,0.15);
        }
        button {
            margin-top: 22px;
            width: 100%;
            padding: 14px;
            background: linear-gradient(135deg, #ff9234, #ff5d6c);
            color: #fff;
            border: none;
            border-radius: 12px;
            font-size: 15px;
            font-weight: 600;
            cursor: pointer;
            transition: opacity 0.2s ease;
        }
        button:hover {
            opacity: 0.92;
        }
    </style>
</head>
<body>
<div class="card">
    <h1>Welcome back</h1>
    <p>Please sign in to continue.</p>
    {{#error}}
    <div class="error">{{.}}</div>
    {{/error}}
    <form method="post" action="/login">
        <label for="username">Username</label>
        <input id="username" name="username" type="text" placeholder="Enter your username" required>

        <label for="password">Password</label>
        <input id="password" name="password" type="password" placeholder="••••••••" required>

        <button type="submit">Sign In</button>
    </form>
</div>
</body>
</html>
